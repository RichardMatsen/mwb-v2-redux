{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run  --browser chrome --reporter mocha-simple-html-reporter",
    "cypress:run2": "cypress run --headed",
    "cypress:open:dev": "CYPRESS_BASE_URL=http://localhost:4200 cypress open"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai-style": "^1.0.3",
    "cypress": "^2.1.0",
    "mocha": "^5.2.0",
    "mocha-simple-html-reporter": "^1.1.0",
    "mochawesome": "^3.0.3"
  }
}
